import React from 'react';
import RateGameComponent from '../components/RateGame';

const GameListScreen = ({ navigation }) => {
    return (
        <RateGameComponent navigation={navigation} />
    );
};

export default GameListScreen;


import { StyleSheet } from 'react-native';


const searchedProfileScreenStyles = StyleSheet.create({
  followMessageButtonRow: {
    flexDirection: 'row',
    justifyContent: 'center',
    gap: 12,
    marginTop: 1,
    marginBottom: 8
  },
  followMessageButton: {
    flex: 1,
    maxWidth: 180
  },
});

export default searchedProfileScreenStyles;
